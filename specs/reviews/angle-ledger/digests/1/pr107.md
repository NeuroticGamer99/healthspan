# PR #107 review ledger digest

- **Branch hash:** `80caab`
- **Branch:** `chore/containment-gate-runs-first`
- **Rounds captured:** 1 (`1d7bc53122e3`)

Composed by `scripts/ledger.py collapse` from the round fragments
this branch accumulated, which the same run deleted (ADR-0072 §8).
Their content is **inlined** below and never linked:
`scripts/check_spec_links.py` resolves every relative markdown link,
so a link to a deleted fragment fails CI. The `Branch hash` and
`Rounds captured` lines above are machine-read: the round numbers so
an allocation survives its own collapse, and the fingerprint beside
each — sha256 over the text inlined below for that round — so a
re-entered collapse can tell a fragment it already captured from the
same fragment amended afterwards.

## Round 1 — chore/containment-gate-runs-first

### Angle record

- **Date:** 2026-09-11
- **Loop:** external
- **Effort:** max
- **Surface:** the six files in `f667d34...38eab5f` — `.claude/skills/land/SKILL.md`,
  `scripts/run_gates.py`, `specs/adr/0080-…md`, `specs/adr/README.md`, `specs/open-questions.md`,
  `tests/test_run_gates.py`
- **Base (resolved):** f667d34ffd51fd4a56b92a9d98682564cfba601b
- **HEAD:** 38eab5ff1cf94f88cc653ace3b20fff6f478c960
- **HEAD tree:** d77d869f135f382b91e7aa009a314adb5e8dd053
- **Diff size at brief time:** 477 lines, and 477 excluding the ledger, this fragment being
  uncommitted when the brief was written. It was committed during the round-1 apply, so a later
  round measuring `origin/main...HEAD` must exclude it (ADR-0072 §8).
- **Lenses dispatched, and each one's status** (`not-run` / `ran-no-report` / `reported`)**:**
  - `/code-review max` — **reported**, 18 findings, no `ReportFindings` call and no per-finding
    verdict labels. Its primary message never reached the orchestrating conversation; fourteen
    findings, two refutations and the cut list were recovered from the agent's own transcript, and
    the report reconciles that with the gap sweep's four. Read the report's provenance note before
    trusting the count.
  - `/codex:adversarial-review` (`gpt-6-astra`) — **reported**, verdict *approve, no material
    findings*. It contradicts the other lens on three of the brief's uncertainties (1, 2, 7) and in
    each case the contradiction is an argument against a measurement; it re-derived none of the
    figures and never examined the first lens's severest finding. Treat it as coverage of a
    disjoint surface, not as a second opinion on the same one.
- **Brief revision stamp:** afc5b874
- **Angles briefed:** interaction between the four local smokes' remedies (priority); the
  economy angle over the ungated prose; the remedy angle over the unreviewed final commit; the
  whole-artifact angle, dispatched separately with no exclusion list
- **Angles executed:** all four, by the first lens, and the priority angle was the highest-yielding
  as briefed — the interaction angle produced the retention-slot eviction (the notice that creates
  the run directory interacting with the early exit and the prune), the `--print`/`--list`
  divergence, and the footer that now names a gate a filtered list need not contain. The remedy
  angle produced seven findings, every one inside an oracle this branch's own four local smokes had
  written or rewritten. The whole-artifact angle reached four pre-existing prose claims, two of them
  outside the diff's files in effect if not in text (ADR-0075 §1's class-3 condition). The economy
  angle produced the cut list's reuse set rather than findings.
- **Briefed but not executed:** none. The second lens ran a *narrower* method than briefed — seven
  targeted test functions in a read-only harness and three in-memory mutations, with neither full
  pytest nor CI rerun — so its approve covers less than the brief asked for, which is recorded here
  rather than counted as an unexecuted angle.
- **Examined:** no affirmative clearance is available for any region. The first lens enumerated
  findings and refutations and did not report clean coverage, so silence on a file is not evidence
  it was read; its own 15-finding cap truncated the list, and it says the cut items are real. Known
  **not** examined: `specs/adr/README.md` (no finding from either lens, which is not a clearance);
  the two lint carve-outs (the pymarkdown `:(exclude)specs/personal/**` pathspec and
  `check_spec_links.py`'s `_resolves_into_personal` / `md_sources` filters), excluded by the brief
  and still owed to the carve-out widening; and CI itself — `ledger-collapsed`, `gitleaks` and
  `test-matrix` did not run on either lens. Treat the remainder as unexamined per ADR-0072 §7.

#### Do-not-re-run, carried into this round

| Excluded | Paths | Cleared at | Cleared by | Evidence |
|---|---|---|---|---|
| _none_ | — | — | — | First round on this branch; the ledger holds no fragment for it, and the four local smokes consume no round number and record no exclusions. |

### Round record

- **Findings:** 18. A further **9** items were named and cut by the review's own
  15-finding cap — counted as the top-level bullets under its "Cut by the review's
  own 15-finding cap" heading, since one of them bundles a reuse set. The review
  states they are real; they are not counted in the tallies below. **7 of the 9 were
  fixed anyway** while the files were open (the present-tense ordinal, the
  `ahead_of_slow` misnomer, the "mostly ASCII" comment, a docstring claiming a
  comparison it did not make, `/land` step 3a's now-vacuous reason, `_context`'s
  post-construction `dry`, and ADR-0070's step-3a manifest). The `python`-vs-`python3`
  spelling is `deferred` — its own open-questions entry argues renaming now lands a
  spelling HELM overturns, and that entry records this firing. Most of the
  reuse/simplification set is `declined`: the two `--list` header assertions and the
  overlapping gate-element tests check different claims, and the second lens judged
  that consolidation optional.
- **Severity tally** (ADR-0072 §6): `silent-wrong` 7, `loud-wrong` 4, `latent` 5,
  `hygiene` 2.
- **Type mix** (§7): `false-claim` 10, `gap` 7, `cosmetic` 1.
- **Assessment:** `real` 18, `wrong` 0, `already-resolved` 0, `unverified` 0 — every
  finding was re-verified against the live code before it was acted on, and none
  fell over.
- **Disposition:** `fixed` 18. The review's altitude note — declare containment-first
  on the `Gate` entry — is `deferred` rather than fixed; it is not one of the 18 and
  it is the only item reaching §4's selector residual. Owner's call, recorded in
  `specs/open-questions.md` with an acceptance criterion and a trigger.
- **Findings inside material this change had itself written:** 13 of 18. The rule
  used: the cited site was created or rewritten by this branch rather than inherited
  from `origin/main`. The 13 are the 8 in `tests/test_run_gates.py` plus ADR-0080
  §5's figures, §5's two-principle enumeration, the `--list` footer, the untouched
  template sections, and the retention interaction the reorder created. The 5
  pre-existing are §4's selector wording, `render_list`'s unguarded print,
  ADR-0075's class-3 condition, §6's divergence clause, and the `--print`/`--list`
  split.
- **Scope mismatch:** none. The report's branch, HEAD and tree hash all matched a
  fresh `git rev-parse` at apply time, with no intervening commits.
- **Convergence call:** land. Reasoning below.

#### Round record, second half — the analysis

**Locus.** `tests/test_run_gates.py` took 8 findings and ADR-0080 took 6 (§3, §4,
§5 twice, §6, and the template sections); `scripts/run_gates.py` took 3,
`specs/adr/0075-…md` 1. No file in scope was reported clean — the review enumerated
findings and refutations and does not claim clean coverage, so the `Examined` field
above records that no region has affirmative clearance.

**Type mix, read.** `false-claim` at 10 of 18 is the branch's signature and not a
coincidence of wording: the change's whole subject is a *claim about ordering*, and
it was installed by editing prose at four sites and oracles at nine. The single
`cosmetic` is the `--list` footer, whose claim was scoped and therefore true.

**Precision.** 18 of 18 survived re-verification. That is unusually high and is
mostly a property of the lens: the review measured nearly every finding before
reporting it, and each Evidence block quotes the measurement.

**Remedy churn — the highest-yield number here.** 2 of 18 remedies needed a second
attempt, and **one of them needed four**: routing the console through a single
guard. Its lineage produced **8 of the 10 findings the four local smoke rounds
returned**, in a strict progression, each round's fix leaving a reachable gap one
step further out — six prints wrapped by hand missing `render_list`; a scan of
`main`'s source blind to `run_gate`, which `main` calls; that scan widened to
`run_gate` and blind to `run_step`, one call below; and a `print(`-substring match
structurally unable to see `sys.stdout.write` at all. What ended it was abandoning
the enumeration: the invariant is now asserted over the module's AST, and *that*
check immediately found a site neither reviewer had named. The remaining two smoke
findings also sat inside this apply's remedies — the six-gate widening's
`path:line:` overclaim, and a mis-cited ADR-0072 subsection in this very fragment.
**All 10 of 10 smoke findings were in remedies; none was in the 18 findings'
original subject matter.**

**Cross-lens delta.** The two lenses were not two opinions on one surface. Lens 1
(`/code-review max`) returned 18 findings; lens 2 (`codex`, `gpt-6-astra`) returned
**approve, no material findings**, and contradicted lens 1 on three of the brief's
seven uncertainties — each time arguing against a measurement it had not re-derived,
and it never examined lens 1's severest finding. Against the local loop the delta is
sharper still: four local smoke rounds ran *before* this round and passed every one
of the 8 test-oracle sites it then found. Local and external rounds again found
disjoint classes.

**Causal read.** One mechanism explains most of this round: a claim was corrected at
the site a reviewer named, and the correction wrote new claims elsewhere. The cost
figure existed at one site on `origin/main` and this change copied it to four; the
ordering ordinal was fixed three times before the fourth site — the owning ADR
section — was found. The same shape drove the console-guard churn, one abstraction
level up: each remedy was a *narrower enumeration of the same kind* as the thing it
replaced. The fix that held was the one that stopped enumerating.

**Convergence call, and what it rests on.** Land. Four smoke rounds inside this
apply moved from a reachable crash (round 1), to a second reachable crash (round 2),
to a checker incomplete at one argument slot (round 3), to a dormant keyword slot in
a test helper with no live instance (round 4). Both reviewers returned `pass` or
`pass with notes` on round 4 and both judged another round unwarranted;
`spec-reviewer` returned a clean `pass` on rounds 3 and 4 with no defect. The
findings have converged from correctness to precision, which is this repository's
recorded signal to stop, since a round that comes back empty never arrives.

**The disclosure that belongs beside that call:** the final commit has been reviewed
by no agent. It applies round 4's two findings and was verified by the orchestrator
only — five mutations run and killed, all 13 local gates green, 261 passed / 1
skipped. Three CI-only gates (`ledger-collapsed`, `gitleaks`, `test-matrix`) have not
run at all; a clean local run is not CI green.
